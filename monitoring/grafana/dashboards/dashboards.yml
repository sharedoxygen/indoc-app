apiVersion: 1
providers:
  - name: inDoc Dashboards
    orgId: 1
    folder: inDoc
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards/indoc
      recursive: true
